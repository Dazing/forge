// Package launcher builds and submits signed run-specs to the
// factory-launcher service, tracking the resulting run rows (status,
// heartbeats, terminal state) in the run table.
//
// This is a Phase 0 placeholder package boundary; its concrete logic lands in
// a later phase. It must not import sibling module packages.
package launcher
