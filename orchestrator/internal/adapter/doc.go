// Package adapter wraps the OpenHands and OpenCode harness process adapters:
// lifecycle, command invocation, and schema-validated artifact exchange.
// Artifacts are stored as bounded references, not inlined.
//
// This is a Phase 0 placeholder package boundary; its concrete logic lands in
// a later phase. It must not import sibling module packages.
package adapter
