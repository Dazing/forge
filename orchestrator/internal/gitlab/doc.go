// Package gitlab is the typed GitLab REST client: request/response types,
// pagination, rate-limit handling, and idempotent mutations (merge, approve,
// label, note) with request-hash dedup against the action table.
//
// This is a Phase 0 placeholder package boundary; its concrete logic lands in
// a later phase. It must not import sibling module packages.
package gitlab
